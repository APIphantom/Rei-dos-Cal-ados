/** Asset da marca (PNG com fundo transparente em `public/`). Usado pelo link da loja (`BrandLogo`). */
export const STORE_LOGO = {
  src: "/logo-rei-dos-calcados.png",
  width: 1024,
  height: 684,
} as const;
