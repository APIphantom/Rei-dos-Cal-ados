# Copie de Project Settings → API (anon public — nunca service_role no browser)
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua_chave_anon

# Opcional: promover admin via POST /api/admin/setup-role (só servidor; não commitar em repo público)
# SUPABASE_SERVICE_ROLE_KEY=
# ADMIN_SETUP_SECRET=

